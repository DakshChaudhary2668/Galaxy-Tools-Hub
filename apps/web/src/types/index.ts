// Re-export shared DTOs from @galaxy/types.
// Do NOT duplicate type definitions here — keep them in the monorepo package.
// Add frontend-only types below as the app grows.

export type * from '@galaxy/types';
